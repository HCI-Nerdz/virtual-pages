= Changelog

== 2026-08-30 -- Selector hub with separate variant pages

* Landing page is a selector only; Edge / Preview / Contrast each have their own URL (`/edge/`, `/preview/`, `/contrast/`).
* Identity trail includes intermediates: `HCI Nerdz / Demos / virtual-pages [/ variant]`.
* Legacy `#/edge` (etc.) hashes redirect to the real pages.

== 2026-08-30 -- Mockups live inside the tabs

* Per-variant mockups are part of each tab face (label + preview + caption), not a second row above the content.
* The tabpanel starts below the chooser — variant title, blurb, and interactive desk are the changing content.

== 2026-08-30 -- Identity strip is site nav only

* Hub breadcrumb `HCI-Nerdz / virtual-pages`: org → demos index, repo → this demo's site home (not GitHub).
* GitHub remains only on the VCS logo line under the strip.

== 2026-08-30 -- Variant tabs align with mockups

* Tab bar uses the same three-column grid as the mockup tiles so each tab lines up with its card.
* Inactive tabs keep tab chrome (muted fill, top radius, track) instead of reading as flat pills.

== 2026-08-30 -- Preview modes outside facsimile

* Snippets / Screenshots toggle moved from inside the Northbazaar preview page to hub chrome above the demo zone.

== 2026-08-30 -- Compact left-aligned mockup tiles

* Variant mockup tiles keep a ~one-third desktop width (`max-width: 20rem`) and left-align when the grid stacks on narrow viewports.

== 2026-08-30 -- Tab bar, mockup chooser, clean facsimile

* Variant titles are a real tab bar; compact per-variant mockups (~3 columns) also select the demo.
* Demo zone stays a placeholder until a tab or mockup is chosen (deep links still mount that variant).
* Preview stack shows hierarchy arrows between cards and into the current page.
* Northbazaar facsimile uses a distinct forest/paper palette so product faces do not read as HCI hub chrome.
* Tour-guide captions removed from inside the facsimile; teaching stays in hub / variant blurbs.
* Soft/Hard badges quieted to in-product chips.

== 2026-08-30 -- Preview stack Cover Flow + content modes

* Preview cards line up when the bar has room; overlap only when packed, with nearer ancestors more open and left cards Y-rotated (Cover Flow spines).
* Snippets vs Screenshots toggle inside the Preview stack variant — text snippets, or faux page captures with a mock “around last click” focus region.
* Hover peek no longer scales or translates (neighbor hit targets stay stable); whole-card shadow is modest; oversized dark veil removed.

== 2026-08-30 -- Edge peeks in a facsimile frame

* Parent layers are thin labeled edge tabs inside a clipped simulated app bezel — not full blank sheets over the demo hub.
* Clicking an edge still climbs the backbone; hub chrome stays outside the facsimile.
* Intro anchor is an inlined SVG (bundled via `?raw`) so the memory-hook cannot 404 behind a dark placeholder box.

== 2026-08-30 -- Soft/hard transport on one backbone

* Decision nodes carry `route` plus `transport` (`soft` | `hard`).
* Edge and preview faces show the route and a soft/hard badge — PDF/invoice stays a hard step inside the same stack.
* Suite copy frames mega-pages as a cost myth once SPA routes exist; flat contrast variant calls that out.

== 2026-08-30 -- Virtual pages desk

* Edge-layered virtual pages (1–2 parent edges, in-page breadcrumb, left up-strip).
* Top-bar preview stack with hover peek (raise + dark veil; later cards hide without layout shift).
* Flat mega-panel contrast; marketing shell outside an armed settings subtree.
* Hub chrome: org/repo identity, GitHub VCS line, suite lede, intro anchor SVG, variant switcher.
* GitHub Pages workflow at `/virtual-pages/`.
